// firecrawl.service.ts

class FirecrawlService {
    extractProductData(html: string) {
        // logic to extract product data from HTML
        return productData;
    }

    saveProductData(productData: object) {
        // logic to save product data to database
    }
}

export default FirecrawlService;